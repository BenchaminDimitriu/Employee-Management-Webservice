<?php
/*	include(); // try to import a file and gives warning if fails
	include_once();// try to import a file (if it was not previously imported) and warn us on failure 
	require();// try to import a file and stop on failure
	require_once();// try to import a file (if it was not previously imported) and stop on failure
	4 types of include
	*/
	require_once('app/core/init.php');

	//the skeleton
	new app\core\App(); //properly named space
